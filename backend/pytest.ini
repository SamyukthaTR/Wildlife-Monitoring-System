[pytest]
# CI invokes the bare `pytest` console script (not `python -m pytest`), which
# does not add the current directory to sys.path -- without this, `from app...`
# imports in tests/conftest.py fail with ModuleNotFoundError.
pythonpath = .
markers =
    integration: requires a live Postgres+PostGIS test database (see docs/milestone4.md)
